local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local CurrentCamera = workspace.CurrentCamera

local Connections = {}
local ESPObjects = {}
local Settings = {
    SpeedValue = 1,
    ShootTiming = 0.8,
    GoodValue = 0.9,
    GreatValue = 0.95,
    BallMagnetDistance = 30,
    OffsetDistance = 0,
    FollowOffset = -10,
    StealReachMultiplier = 1.5,
    PostActivationDistance = 10,
    QuickTpDist = 2.5,
    QuickTpDelay = 0.3,
    RToTpDist = 2.5,
    RToTpDelay = 0.3,
    FOVValue = 90,
    CameraResValue = 0.8
}

local function GetCharacter()
    return LocalPlayer.Character
end

local function GetHumanoid()
    local char = GetCharacter()
    return char and char:FindFirstChild("Humanoid")
end

local function GetHumanoidRootPart()
    local char = GetCharacter()
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function CreateConnection(name, connection)
    if Connections[name] then
        Connections[name]:Disconnect()
    end
    Connections[name] = connection
    return connection
end

local function DisconnectConnection(name)
    if Connections[name] then
        Connections[name]:Disconnect()
        Connections[name] = nil
    end
end

local function GetPlayerWithBall()
    for _, player in pairs(Players:GetPlayers()) do
        if player.Character and player.Character:FindFirstChild("Basketball") then
            return player
        end
    end
    return nil
end

local function CreateESPLine()
    local line = Drawing.new("Line")
    line.Visible = false
    line.Color = Color3.fromRGB(255, 0, 0)
    line.Thickness = 2
    line.ZIndex = 1
    return line
end

local function CreateHighlight(parent)
    local highlight = Instance.new("Highlight")
    highlight.Name = "PlayerESP"
    highlight.FillColor = Color3.fromRGB(0, 255, 0)
    highlight.OutlineColor = Color3.fromRGB(0, 0, 255)
    highlight.FillTransparency = 0.7
    highlight.OutlineTransparency = 0
    highlight.Parent = parent
    return highlight
end

local function CleanupESP()
    for _, obj in pairs(ESPObjects) do
        if obj.Remove then
            obj:Remove()
        elseif obj.Destroy then
            obj:Destroy()
        end
    end
    ESPObjects = {}
end

local function SetupFullBright()
    Lighting.Ambient = Color3.fromRGB(204, 204, 204)
    Lighting.Brightness = 1.5
    Lighting.GlobalShadows = false
    Lighting.OutdoorAmbient = Color3.fromRGB(204, 204, 204)
end

local function SetupNoFog()
    Lighting.FogEnd = 1000000
    Lighting.FogStart = 1000000
end

local function SetupNVIDIAShaders()
    for _, effect in pairs(Lighting:GetChildren()) do
        if effect:IsA("PostEffect") then
            effect:Destroy()
        end
    end

    local bloom = Instance.new("BloomEffect")
    bloom.Name = "NVIDIABloom"
    bloom.Intensity = 0.2
    bloom.Size = 16
    bloom.Threshold = 0.9
    bloom.Parent = Lighting

    local colorCorrection = Instance.new("ColorCorrectionEffect")
    colorCorrection.Name = "NVIDIAColor"
    colorCorrection.Brightness = 0.02
    colorCorrection.Contrast = 0.1
    colorCorrection.Saturation = 0.1
    colorCorrection.TintColor = Color3.fromRGB(257, 256, 253)
    colorCorrection.Parent = Lighting

    local depth = Instance.new("DepthOfFieldEffect")
    depth.Name = "NVIDIADepth"
    depth.FarIntensity = 0.02
    depth.FocusDistance = 25
    depth.InFocusRadius = 40
    depth.NearIntensity = 0.1
    depth.Parent = Lighting

    local sunRays = Instance.new("SunRaysEffect")
    sunRays.Name = "NVIDIASunRays"
    sunRays.Intensity = 0.1
    sunRays.Spread = 0.4
    sunRays.Parent = Lighting

    local blur = Instance.new("BlurEffect")
    blur.Name = "NVIDIABlur"
    blur.Size = 0.2
    blur.Parent = Lighting

    Lighting.GlobalShadows = true
    Lighting.ShadowSoftness = 0.4
    Lighting.Brightness = 1.05
    Lighting.OutdoorAmbient = Color3.fromRGB(153, 178, 229)
    Lighting.Ambient = Color3.fromRGB(114, 114, 140)
    Lighting.ColorShift_Bottom = Color3.fromRGB(5, 5, 12)
    Lighting.ColorShift_Top = Color3.fromRGB(204, 178, 153)
    Lighting.FogColor = Color3.fromRGB(102, 127, 178)
    Lighting.FogStart = 100
    Lighting.FogEnd = 500

    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("Part") then
            if obj.Material == Enum.Material.Plastic or obj.Material == Enum.Material.Wood then
            end
        end
    end
end

local Window = Rayfield:CreateWindow({
    LoadingTitle = "Loading...",
    LoadingSubtitle = "by Leakg0d & Eon!",
    Name = "Flash Hub | Basketball Legends",
    Theme = {
        Shadow = Color3.fromRGB(5, 10, 20),
        SliderProgress = Color3.fromRGB(100, 150, 255),
        PlaceholderColor = Color3.fromRGB(140, 180, 255),
        InputStroke = Color3.fromRGB(70, 110, 190),
        ToggleDisabledStroke = Color3.fromRGB(60, 60, 60),
        InputBackground = Color3.fromRGB(15, 25, 45),
        ElementBackgroundHover = Color3.fromRGB(30, 45, 80),
        DropdownUnselected = Color3.fromRGB(20, 30, 55),
        SelectedTabTextColor = Color3.fromRGB(120, 170, 255),
        NotificationBackground = Color3.fromRGB(20, 30, 55),
        DropdownSelected = Color3.fromRGB(30, 45, 80),
        SecondaryElementStroke = Color3.fromRGB(50, 90, 160),
        Background = Color3.fromRGB(10, 15, 30),
        ToggleDisabledOuterStroke = Color3.fromRGB(40, 40, 40),
        TabStroke = Color3.fromRGB(50, 70, 120),
        ElementBackground = Color3.fromRGB(20, 30, 55),
        ToggleEnabledOuterStroke = Color3.fromRGB(50, 90, 160),
        ToggleEnabled = Color3.fromRGB(100, 150, 255),
        ToggleEnabledStroke = Color3.fromRGB(70, 120, 200),
        ToggleDisabled = Color3.fromRGB(90, 90, 90),
        SecondaryElementBackground = Color3.fromRGB(15, 25, 45),
        ToggleBackground = Color3.fromRGB(20, 25, 45),
        TabTextColor = Color3.fromRGB(170, 200, 255),
        ElementStroke = Color3.fromRGB(70, 110, 180),
        SliderBackground = Color3.fromRGB(40, 70, 120),
        SliderStroke = Color3.fromRGB(70, 120, 200),
        NotificationActionsBackground = Color3.fromRGB(35, 50, 80),
        Topbar = Color3.fromRGB(15, 25, 45),
        TabBackground = Color3.fromRGB(40, 60, 100),
        TabBackgroundSelected = Color3.fromRGB(25, 40, 80),
        TextColor = Color3.fromRGB(170, 200, 255),
    },
})

local BallModsTab = Window:CreateTab("Ball Mods")

BallModsTab:CreateSection("Auto Shooting")

BallModsTab:CreateToggle({
    Name = "Auto Time",
    Flag = "AutoShoot",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            local PlayerGui = LocalPlayer.PlayerGui
            local Visual = PlayerGui:WaitForChild("Visual")
            local Shooting = Visual:WaitForChild("Shooting")
            
            CreateConnection("AutoShoot", Shooting:GetPropertyChangedSignal("Visible"):Connect(function()
                if Shooting.Visible then
                    local timing = Settings.ShootTiming
                    wait(0.1)
                end
            end))
        else
            DisconnectConnection("AutoShoot")
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Shot Timing",
    Flag = "ShootTiming",
    Suffix = "%",
    CurrentValue = 80,
    Range = {50, 100},
    Increment = 1,
    Callback = function(value)
        Settings.ShootTiming = value / 100
    end,
})

BallModsTab:CreateLabel("I recommend using this instead of the Auto Green!")
BallModsTab:CreateLabel("80 = Mediocre | 90 = Good | 95 = Great | 100 = Perfect")

BallModsTab:CreateSection("Auto Green")
BallModsTab:CreateLabel("The Randomizer goes from Good, Great, Perfect! (Change to 1 for Perfect)")

BallModsTab:CreateToggle({
    Name = "Randomizer",
    Flag = "Randomizer",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("Randomizer", RunService.RenderStepped:Connect(function()
                local PlayerGui = LocalPlayer.PlayerGui
                local Visual = PlayerGui:WaitForChild("Visual")
                local Shooting = Visual:WaitForChild("Shooting")
                
                if Shooting.Visible then
                    local random = math.random()
                    if random > Settings.GreatValue then
                    elseif random > Settings.GoodValue then
                    end
                end
            end))
        else
            DisconnectConnection("Randomizer")
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Good Value",
    Flag = "GoodValue",
    Suffix = "value",
    CurrentValue = 0.9,
    Range = {0.1, 1},
    Increment = 0.01,
    Callback = function(value)
        Settings.GoodValue = value
    end,
})

BallModsTab:CreateSlider({
    Name = "Great Value",
    Flag = "GreatValue",
    Suffix = "value",
    CurrentValue = 0.95,
    Range = {0.1, 1},
    Increment = 0.01,
    Callback = function(value)
        Settings.GreatValue = value
    end,
})

BallModsTab:CreateSection("Ball Magnet")

BallModsTab:CreateToggle({
    Name = "Ball Magnet",
    Flag = "BallMagnet",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("BallMagnet", RunService.Heartbeat:Connect(function()
                local hrp = GetHumanoidRootPart()
                if not hrp then return end
                
                for _, obj in pairs(workspace:GetChildren()) do
                    if obj.Name == "Basketball" and obj:IsA("Tool") then
                        local handle = obj:FindFirstChild("Handle")
                        if handle and (handle.Position - hrp.Position).Magnitude <= Settings.BallMagnetDistance then
                        end
                    end
                end
            end))
        else
            DisconnectConnection("BallMagnet")
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Magnet Distance",
    Flag = "BallMagnetDistance",
    Suffix = " studs",
    CurrentValue = 30,
    Range = {10, 30},
    Increment = 1,
    Callback = function(value)
        Settings.BallMagnetDistance = value
    end,
})

BallModsTab:CreateSection("Auto Guard")

BallModsTab:CreateToggle({
    Name = "Auto Guard",
    Flag = "AutoGuard",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("AutoGuard", RunService.Heartbeat:Connect(function()
                local hrp = GetHumanoidRootPart()
                local char = GetCharacter()
                if not hrp or not char then return end
                
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        local basketball = player.Character:FindFirstChild("Basketball")
                        if basketball and player.Team ~= LocalPlayer.Team then
                            local targetHRP = player.Character:FindFirstChild("HumanoidRootPart")
                            if targetHRP then
                                hrp.CFrame = CFrame.lookAt(hrp.Position, Vector3.new(targetHRP.Position.X, hrp.Position.Y, targetHRP.Position.Z))
                                
                                pcall(function()
                                    for _, tool in pairs(char:GetChildren()) do
                                        if tool:IsA("Tool") and tool.Name:lower():find("guard") then
                                            tool:Activate()
                                        end
                                    end
                                end)
                            end
                        end
                    end
                end
            end))
        else
            DisconnectConnection("AutoGuard")
        end
    end,
})

BallModsTab:CreateLabel("This will auto face the character to the person that has the ball, so this makes it easier for you to Block & Guard!")

BallModsTab:CreateSection("Auto Rebound & Steal")

BallModsTab:CreateToggle({
    Name = "Auto Rebound & Steal V2",
    Flag = "ReboundAutoSteal",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("ReboundAutoSteal", RunService.Heartbeat:Connect(function()
                local hrp = GetHumanoidRootPart()
                if not hrp then return end
                
                for _, obj in pairs(workspace:GetChildren()) do
                    if obj.Name == "Basketball" and not obj.Parent:FindFirstChild("Humanoid") then
                        local handle = obj:FindFirstChild("Handle")
                        if handle then
                            local offset = Settings.OffsetDistance
                            hrp.CFrame = CFrame.new(handle.Position + Vector3.new(0, offset, 0))
                        end
                    end
                end
            end))
        else
            DisconnectConnection("ReboundAutoSteal")
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Rebound Offset Distance",
    Flag = "OffsetDistance",
    Suffix = " studs",
    CurrentValue = 0,
    Range = {0, 6},
    Increment = 0.1,
    Callback = function(value)
        Settings.OffsetDistance = value
    end,
})

BallModsTab:CreateSection("Follow Ball Carrier")

BallModsTab:CreateToggle({
    Name = "Follow Ball Carrier",
    Flag = "FollowBallCarrier",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("FollowBallCarrier", RunService.Heartbeat:Connect(function()
                local hrp = GetHumanoidRootPart()
                if not hrp then return end
                
                for _, player in pairs(Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character then
                        local basketball = player.Character:FindFirstChild("Basketball")
                        if basketball and basketball:IsA("Tool") then
                            local targetHRP = player.Character:FindFirstChild("HumanoidRootPart")
                            if targetHRP then
                                local direction = (hrp.Position - targetHRP.Position).Unit
                                local offset = Settings.FollowOffset
                                hrp.CFrame = CFrame.new(targetHRP.Position + direction * offset)
                            end
                        end
                    end
                end
            end))
        else
            DisconnectConnection("FollowBallCarrier")
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Follow Offset",
    Flag = "FollowOffset",
    Suffix = " studs",
    CurrentValue = -10,
    Range = {-10, 10},
    Increment = 1,
    Callback = function(value)
        Settings.FollowOffset = value
    end,
})

BallModsTab:CreateSection("Steal Reach")

BallModsTab:CreateToggle({
    Name = "Steal Reach",
    Flag = "StealReach",
    CurrentValue = false,
    Callback = function(enabled)
        local char = GetCharacter()
        if not char then return end
        
        local rightArm = char:FindFirstChild("Right Arm")
        local leftArm = char:FindFirstChild("Left Arm")
        
        if enabled then
            if rightArm then
                local size = 4.5 * Settings.StealReachMultiplier
                rightArm.Size = Vector3.new(size, size, size)
                rightArm.Transparency = 1
                rightArm.CanCollide = false
                rightArm.Massless = true
            end
            if leftArm then
                local size = 4.5 * Settings.StealReachMultiplier
                leftArm.Size = Vector3.new(size, size, size)
                leftArm.Transparency = 1
                leftArm.CanCollide = false
                leftArm.Massless = true
            end
        else
            if rightArm then
                rightArm.Size = Vector3.new(1, 2, 1)
                rightArm.Transparency = 0
                rightArm.CanCollide = true
                rightArm.Massless = false
            end
            if leftArm then
                leftArm.Size = Vector3.new(1, 2, 1)
                leftArm.Transparency = 0
                leftArm.CanCollide = true
                leftArm.Massless = false
            end
        end
    end,
})

BallModsTab:CreateSlider({
    Name = "Steal Reach Multiplier",
    Flag = "StealReachMultiplier",
    Suffix = "x",
    CurrentValue = 1.5,
    Range = {1, 10},
    Increment = 0.1,
    Callback = function(value)
        Settings.StealReachMultiplier = value
    end,
})

BallModsTab:CreateLabel("Steal Reach should work 100%!")

local PostAimbotTab = Window:CreateTab("Post Aimbot")

PostAimbotTab:CreateSection("Post Aimbot")

PostAimbotTab:CreateToggle({
    Name = "Post Aimbot",
    Flag = "PostAimbot",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("PostAimbot", RunService.Heartbeat:Connect(function()
                local hrp = GetHumanoidRootPart()
                if not hrp then return end
                
                local nearestHoop = nil
                local nearestDistance = math.huge
                
                for _, obj in pairs(workspace:GetDescendants()) do
                    if obj.Name == "Hoop" or obj.Name == "Rim" then
                        local distance = (obj.Position - hrp.Position).Magnitude
                        if distance < nearestDistance and distance <= Settings.PostActivationDistance then
                            nearestDistance = distance
                            nearestHoop = obj
                        end
                    end
                end
                
                if nearestHoop then
                end
            end))
        else
            DisconnectConnection("PostAimbot")
        end
    end,
})

PostAimbotTab:CreateSlider({
    Name = "Activation Distance",
    Flag = "PostActivationDistance",
    Suffix = " studs",
    CurrentValue = 10,
    Range = {5, 20},
    Increment = 1,
    Callback = function(value)
        Settings.PostActivationDistance = value
    end,
})

PostAimbotTab:CreateLabel("Automatically detects which hand has the ball, and posts accordingly!")

local PlayerConfigTab = Window:CreateTab("Player Configuration")

PlayerConfigTab:CreateSection("Speed Boost")

PlayerConfigTab:CreateToggle({
    Name = "Speed Boost",
    Flag = "SpeedEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("SpeedBoost", RunService.RenderStepped:Connect(function()
                local hrp = GetHumanoidRootPart()
                local humanoid = GetHumanoid()
                if not hrp or not humanoid then return end
                
                if humanoid.MoveDirection.Magnitude > 0 then
                    hrp.CFrame = hrp.CFrame + humanoid.MoveDirection * Settings.SpeedValue * 0.1
                end
            end))
        else
            DisconnectConnection("SpeedBoost")
        end
    end,
})

PlayerConfigTab:CreateSlider({
    Name = "Speed Value",
    Flag = "SpeedValue",
    Suffix = "x",
    CurrentValue = 1,
    Range = {0.1, 2},
    Increment = 0.1,
    Callback = function(value)
        Settings.SpeedValue = value
    end,
})

PlayerConfigTab:CreateSection("Mobile Teleports")

local lastTeleport = 0

local function CreateQuickTPButton()
    local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
    
    if PlayerGui:FindFirstChild("TpButton") then
        PlayerGui.TpButton:Destroy()
    end
    
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "TpButton"
    screenGui.Parent = PlayerGui
    
    local button = Instance.new("TextButton")
    button.Name = "Btn"
    button.Size = UDim2.new(0, 200, 0, 50)
    button.Position = UDim2.new(0.5, -100, 0.9, -25)
    button.AnchorPoint = Vector2.new(0.5, 0.5)
    button.BackgroundTransparency = 1
    button.Text = ""
    button.Parent = screenGui
    
    local bg = Instance.new("Frame")
    bg.Name = "Bg"
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    bg.Parent = button
    
    local gradient = Instance.new("UIGradient")
    gradient.Rotation = 90
    gradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 100, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(150, 0, 255))
    })
    gradient.Parent = bg
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 12)
    corner.Parent = bg
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(60, 60, 60)
    stroke.Thickness = 2
    stroke.Parent = bg
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = "Quick Tp"
    label.Font = Enum.Font.GothamBold
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 20
    label.Parent = button
    
    local labelStroke = Instance.new("UIStroke")
    labelStroke.Color = Color3.fromRGB(0, 0, 0)
    labelStroke.Thickness = 1.5
    labelStroke.Parent = label
    
    button.MouseEnter:Connect(function()
        TweenService:Create(gradient, TweenInfo.new(0.2), {
            Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(50, 150, 255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 50, 255))
            })
        }):Play()
        TweenService:Create(stroke, TweenInfo.new(0.2), {Thickness = 3}):Play()
    end)
    
    button.MouseLeave:Connect(function()
        TweenService:Create(gradient, TweenInfo.new(0.3), {
            Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 100, 255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(150, 0, 255))
            })
        }):Play()
        TweenService:Create(stroke, TweenInfo.new(0.3), {Thickness = 2}):Play()
    end)
    
    button.MouseButton1Click:Connect(function()
        local currentTime = tick()
        if currentTime - lastTeleport < Settings.QuickTpDelay then return end
        lastTeleport = currentTime
        
        local hrp = GetHumanoidRootPart()
        local humanoid = GetHumanoid()
        if not hrp or not humanoid then return end
        
        if humanoid.MoveDirection.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + humanoid.MoveDirection * Settings.QuickTpDist
        end
    end)
end

PlayerConfigTab:CreateToggle({
    Name = "Quick TP",
    Flag = "QuickTpEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateQuickTPButton()
        else
            local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
            if PlayerGui:FindFirstChild("TpButton") then
                PlayerGui.TpButton:Destroy()
            end
        end
    end,
})

PlayerConfigTab:CreateSlider({
    Name = "Quick TP Distance",
    Flag = "QuickTpDist",
    Suffix = "studs",
    CurrentValue = 2.5,
    Range = {0.1, 5},
    Increment = 0.1,
    Callback = function(value)
        Settings.QuickTpDist = value
    end,
})

PlayerConfigTab:CreateSlider({
    Name = "Quick TP Delay",
    Flag = "QuickTpDelay",
    Suffix = "seconds",
    CurrentValue = 0.3,
    Range = {0.1, 1},
    Increment = 0.1,
    Callback = function(value)
        Settings.QuickTpDelay = value
    end,
})

PlayerConfigTab:CreateSection("Computer Teleports")

local lastRTeleport = 0

PlayerConfigTab:CreateToggle({
    Name = "R to TP",
    Flag = "RToTpEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("RTeleport", UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if gameProcessed then return end
                if input.KeyCode == Enum.KeyCode.R then
                    local currentTime = tick()
                    if currentTime - lastRTeleport < Settings.RToTpDelay then return end
                    lastRTeleport = currentTime
                    
                    local hrp = GetHumanoidRootPart()
                    local humanoid = GetHumanoid()
                    if not hrp or not humanoid then return end
                    
                    if humanoid.MoveDirection.Magnitude > 0 then
                        hrp.CFrame = hrp.CFrame + humanoid.MoveDirection * Settings.RToTpDist
                    end
                end
            end))
        else
            DisconnectConnection("RTeleport")
        end
    end,
})

PlayerConfigTab:CreateSlider({
    Name = "R TP Distance",
    Flag = "RToTpDist",
    Suffix = "studs",
    CurrentValue = 2.5,
    Range = {0.1, 3},
    Increment = 0.1,
    Callback = function(value)
        Settings.RToTpDist = value
    end,
})

PlayerConfigTab:CreateSlider({
    Name = "R TP Delay",
    Flag = "RToTpDelay",
    Suffix = "seconds",
    CurrentValue = 0.3,
    Range = {0.1, 1},
    Increment = 0.1,
    Callback = function(value)
        Settings.RToTpDelay = value
    end,
})

PlayerConfigTab:CreateLabel("Click R to teleport if you are in a Computer!")

local ESPTab = Window:CreateTab("ESP Features")

ESPTab:CreateSection("Tracers")

ESPTab:CreateToggle({
    Name = "Tracer ESP",
    Flag = "TracerEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local line = CreateESPLine()
                    ESPObjects[player] = line
                end
            end
            
            CreateConnection("TracerESP", RunService.RenderStepped:Connect(function()
                for player, line in pairs(ESPObjects) do
                    if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local hrp = player.Character.HumanoidRootPart
                        local vector, onScreen = CurrentCamera:WorldToViewportPoint(hrp.Position)
                        
                        if onScreen then
                            line.From = Vector2.new(CurrentCamera.ViewportSize.X / 2, CurrentCamera.ViewportSize.Y)
                            line.To = Vector2.new(vector.X, vector.Y)
                            line.Visible = true
                        else
                            line.Visible = false
                        end
                    else
                        line.Visible = false
                    end
                end
            end))
        else
            DisconnectConnection("TracerESP")
            CleanupESP()
        end
    end,
})

ESPTab:CreateSection("Player ESP")

ESPTab:CreateToggle({
    Name = "Player ESP",
    Flag = "PlayerESP",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local highlight = CreateHighlight(player.Character)
                    ESPObjects[player] = highlight
                end
            end
            
            CreateConnection("PlayerESPUpdate", Players.PlayerAdded:Connect(function(player)
                player.CharacterAdded:Connect(function(char)
                    wait(0.5)
                    if ESPObjects[player] then
                        ESPObjects[player]:Destroy()
                    end
                    ESPObjects[player] = CreateHighlight(char)
                end)
            end))
        else
            DisconnectConnection("PlayerESPUpdate")
            CleanupESP()
        end
    end,
})

local WorldTab = Window:CreateTab("World Changer")

WorldTab:CreateSection("FOV Modifier")

WorldTab:CreateToggle({
    Name = "FOV Modifier",
    Flag = "FOVEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("FOVModifier", RunService.RenderStepped:Connect(function()
                CurrentCamera.FieldOfView = Settings.FOVValue
            end))
        else
            DisconnectConnection("FOVModifier")
            CurrentCamera.FieldOfView = 70
        end
    end,
})

WorldTab:CreateSlider({
    Name = "FOV Value",
    Flag = "FOVValue",
    Suffix = "FOV",
    CurrentValue = 90,
    Range = {60, 120},
    Increment = 1,
    Callback = function(value)
        Settings.FOVValue = value
        CurrentCamera.FieldOfView = value
    end,
})

WorldTab:CreateSection("Camera Resolution")

WorldTab:CreateToggle({
    Name = "Camera Resolution Toggle",
    Flag = "CameraResEnabled",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            CreateConnection("CameraRes", RunService.RenderStepped:Connect(function()
                CurrentCamera.CFrame = CurrentCamera.CFrame * CFrame.new(0, 0, 0, 1, 0, 0, 0, Settings.CameraResValue, 0, 0, 0, 1)
            end))
        else
            DisconnectConnection("CameraRes")
        end
    end,
})

WorldTab:CreateSlider({
    Name = "Camera Resolution",
    Flag = "CameraResValue",
    Suffix = "quality",
    CurrentValue = 0.8,
    Range = {0.3, 1},
    Increment = 0.05,
    Callback = function(value)
        Settings.CameraResValue = value
    end,
})

WorldTab:CreateSection("Extra Stuff")

WorldTab:CreateToggle({
    Name = "Full Bright",
    Flag = "FullBright",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            SetupFullBright()
        else
            Lighting.Ambient = Color3.fromRGB(0, 0, 0)
            Lighting.Brightness = 1
            Lighting.GlobalShadows = true
            Lighting.OutdoorAmbient = Color3.fromRGB(127, 127, 127)
        end
    end,
})

WorldTab:CreateToggle({
    Name = "No Fog",
    Flag = "NoFog",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            SetupNoFog()
        else
            Lighting.FogEnd = 100000
            Lighting.FogStart = 0
        end
    end,
})

WorldTab:CreateToggle({
    Name = "NVIDIA Style Shaders",
    Flag = "Shaders",
    CurrentValue = false,
    Callback = function(enabled)
        if enabled then
            SetupNVIDIAShaders()
        else
            for _, effect in pairs(Lighting:GetChildren()) do
                if effect.Name:find("NVIDIA") then
                    effect:Destroy()
                end
            end
            Lighting.Brightness = 1
            Lighting.GlobalShadows = true
            Lighting.ShadowSoftness = 0.2
        end
    end,
})

LocalPlayer.CharacterAdded:Connect(function(char)
    wait(1)
    
    Character = char
    Humanoid = char:WaitForChild("Humanoid")
    HumanoidRootPart = char:WaitForChild("HumanoidRootPart")
    
    for name, _ in pairs(Connections) do
        DisconnectConnection(name)
    end
    
    CleanupESP()
end)

Players.PlayerAdded:Connect(function(player)
    if ESPObjects[player] then return end
    
    player.CharacterAdded:Connect(function(char)
        wait(0.5)
        local PlayerESPEnabled = Rayfield.Flags["PlayerESP"]
        if PlayerESPEnabled and PlayerESPEnabled.CurrentValue then
            ESPObjects[player] = CreateHighlight(char)
        end
        
        local TracerEnabled = Rayfield.Flags["TracerEnabled"]
        if TracerEnabled and TracerEnabled.CurrentValue then
            ESPObjects[player] = CreateESPLine()
        end
    end)
end)

Players.PlayerRemoving:Connect(function(player)
    if ESPObjects[player] then
        if ESPObjects[player].Remove then
            ESPObjects[player]:Remove()
        elseif ESPObjects[player].Destroy then
            ESPObjects[player]:Destroy()
        end
        ESPObjects[player] = nil
    end
end)

Rayfield:LoadConfiguration()